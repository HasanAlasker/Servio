import { View, StyleSheet } from "react-native";
import CardComp from "./CardComp";

function ReportCard(props) {
  return <CardComp></CardComp>;
}

const styles = StyleSheet.create({
  container: {},
});

export default ReportCard;
